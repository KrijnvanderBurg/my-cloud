---
# ---------------------------------------------------------------------------
# Global variables shared across all hosts (hypervisors + VMs)
# ---------------------------------------------------------------------------
environment: dev
managed_by: ansible
project: levendaal
layer: base-infra

# Naming convention: <role>-<workload>-<env>-<location>-<instance>
naming_prefix: "base-infra-dev-onprem"

# SSH key directory – auto-generated by 01-hypervisor-setup role
vm_ssh_key_dir: "~/.ssh/base-infra-{{ environment }}"
vm_ssh_key_name: "id_ed25519"
