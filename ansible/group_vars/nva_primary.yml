---
# Group variables for NVA hosts

# pfSense API settings (if using pfSense API)
pfsense_api_enabled: false

# CARP settings
carp_password: "{{ vault_carp_password }}"
carp_vhid: 1
carp_advskew_primary: 0
carp_advskew_secondary: 100

# Firewall rule defaults
default_firewall_action: pass
log_default_deny: true

# Spoke networks (populated per region)
spoke_networks: []
