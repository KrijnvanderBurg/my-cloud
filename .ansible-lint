---
# ansible-lint configuration
# https://ansible.readthedocs.io/projects/lint/configuring/

profile: production

exclude_paths:
  - .devcontainer/
  - .github/
  - 02-azure/
  - 03-ovhcloud/
  - docs/

skip_list:
  - name[template] # Allow Jinja2 in task names if needed

enable_list:
  - fqcn
